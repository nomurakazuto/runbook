<div class="row text-center">
    <div class="offset-sm-2 col-sm-8">
        <h1>タスク編集ページ</h1>

        <%= render 'form', task: @task %>
        
        <%=link_to "タスク詳細に戻る", task_path(@task), class: "mt-4 btn btn-success"%>
    </div>
    
</div>